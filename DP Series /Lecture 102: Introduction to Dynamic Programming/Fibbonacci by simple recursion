#include<iostream>
using namespace std;
int fib(int n)
{
    if(n==0 || n==1)
    {
        return n;
    }
    
    int ans=0;
     for(int i=0;i<n;i++)
    {
       ans=fib(n-1)+fib(n-2); 
        return ans;
    }
   
}
int main()
{
     int n=6;
    cout << fib(n) << endl;
       return 0;
}
